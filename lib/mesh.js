"use strict";

var log = require('./log')("[mesh] ", "error");

var exports = module.exports = {
  // ... here comes the interface

  // Expose logging for changing log levels.
  _log: log
};

var mesh = {
  // private properties
};

